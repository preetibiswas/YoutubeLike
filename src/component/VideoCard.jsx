/* eslint-disable react/prop-types */
import { Box } from '@mui/material'
import React from 'react'

const VideoCard = ({item}) => {
    console.log('item',item)
  return (
    <Box sx={{color:'#fff',background:'red'}}>VideoCard</Box>
  )
}

export default VideoCard